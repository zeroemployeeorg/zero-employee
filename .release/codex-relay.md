# Codex relay (minor)

kind: minor

Seat-instance relay ledger (`zeo relay`), operator-started Codex supervisor
(`zeo relay start`), worktree lifecycle (`zeo workspace`), `zeo mcp-server`,
`zeo doctor --codex`, `zeo test-runtime codex`, Codex-native `AGENTS.md` /
skills / `.codex/config.toml`, and a narrow R1 reopen for the supervisor
(not a Sandcastle harness). Execution-contracts A1–A6 remain in this minor.
